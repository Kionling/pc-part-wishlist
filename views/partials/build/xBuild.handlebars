<li>
	{{name}}

	<button class="change-status" data-id="{{id}}" data-status="{{pcstatus}}">
		{{#if pcstatus}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-part" data-id="{{this.id}}">DELETE!</button>
</li>
